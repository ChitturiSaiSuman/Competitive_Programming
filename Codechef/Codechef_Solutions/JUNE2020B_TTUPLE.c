#include<stdio.h>
#include<stdlib.h>
#define and &&
int solve(int a, int b, int c, int p, int q, int r)
{
    int ans=3;
    if(a==p and b==q and c==r)
        return 0;
    else
    {
        
    }
        
}